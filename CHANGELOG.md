# 変更履歴 (Changelog)

本プロジェクトのバージョンごとの変更内容です。日本語の紹介ページは [README.md](README.md) / English page: [README-EN.md](README-EN.md) を参照してください。

## 2026-09-21（対象ガード＋Excel・CSV取り込みパック・feature/drawing-search・未リリース）

### 対象ガード（生成前の前提検証・t74/t76型「対象置換」捏造の防御）

- **手続き・可否質問（「〜できますか」「〜する方法」「〜落とせますか」等）で、質問の対象語（助詞直前の3文字以上の漢語・英数字）が取得文書のどれにも現れない場合、生成前に「該当する記載がありません」へ倒す**（guard="target"）: QA文書の類似手続きを別対象へ転用した回答（t74: パスワード再発行→健康保険証再発行。両階級で同一誤答・プロンプト耐性あり）と、規定に無い許可をでっち上げる回答（t76: 個人旅行を「経費可能」）を解消
- **過剰拒否の回避**: 方法/手順/手続きで終わる語（質問の聞き方）は対象外（t18実測で誤発火→修正）・1つでも文書に現れる対象があれば通す・意味的近傍（「公差」の問いに「±0.2」表記）は言い換え許容
- 実測: t74/t76ともfresh状態で確定拒否。118問はガード適用前104→適用後106/118（t18誤発火の一時低下を修正して回復・ベースライン103比+3）

### Excel・CSV取り込みパック（拡張パック第2弾・設定からON/OFF・既定OFF）

- ONで .xlsx / .csv / .tsv をナレッジに取り込み可能。**ヘッダ付き30行バッチのチャンク化**（ヘッダは全チャンクに複製し列の意味を保持）。CSVはUTF-8→Shift-JIS自動判別。xlsxはOpenXML最小解析（ZipArchive+XmlReader・新依存ゼロ）。元ファイルも保存（「開く」可）
- 既知限界（v1）: 日付セルはシリアル値のまま・数式は保存値・.xls（旧バイナリ）非対応
- 実測: CSV取り込み→「サポートブラケットの数量は？」に表から正答（出典付き）
- 単体テスト 88→**104**（対象ガード・SheetExtract追加）

## 2026-09-21（v2.1.6・図面PDF検索・Q&A 拡張パック）

> 内容: 拡張パック本体・回答品質修正（fact-grounding-8）・v2.1.5実機検証の結果。コードはv2.1.5検証済みビルドと同一（以降はドキュメント整備のみ）

### 製造業向け拡張パック「図面PDF検索・Q&A」 — 設定からON/OFF（既定OFF・本体フロー不変）

- **PDFテキスト抽出をPdfPigに置換**（従来の自製抽出器はフォールバックとして残存）: CJK CIDフォントの日本語PDF（CAD出力PDFを含む）で文字化け・抽出失敗していた本体品質の改善。拡張OFFでも常時有効
- **拡張パック機構**: 設定画面「拡張機能」カードでON/OFF（即時反映・再起動不要）。OFFの間は取り込み・UI・APIが従来と完全同一。将来の業界パック（Excel/CSV取り込み等）も同じ機構に乗る
- **図面取り込み（パックON時）**: 図面PDFを自動判定（ページ数・テキスト量・図番パターンのヒューリスティック）し、表題欄から図番・品名・材質・改訂を抽出してメタデータ化（ルールベース＋ローカルLLM構造化の2段構え）。元PDFとサムネイルを `data/files/` に保存
- **図番の表記ゆれ吸収**: トークナイザに記号結合英数トークン＋正規形（NFKC→英数以外除去）を追加。`A-1234`／`A1234`／`Ａ−１２３４` が同一トークンで一致。トークンは起動時計算のため**再索引・回答キャッシュ無効化は不要**
- **キャプチャ入力**: `Win+Shift+S` → 貼り付けで画像をOCR（Windows内蔵エンジン・完全オフライン・モデル追加なし）し、図番候補を確認チップ（1タップ修正可）で質問に前置き
- ナレッジ一覧: サムネイル・図面バッジ・図番/品名列・フィルタ（`?q=`）。出典: 図番＋品名＋改訂＋「開く」（元PDFを別タブ）
- CI（release.yml）に `dotnet test` ゲートを追加
- ライセンス: PdfPig (Apache-2.0)・Docnet.Core/PDFium (MIT/BSD-3) を THIRD-PARTY-NOTICES に追記

### v2.1.4取り込みと回答品質の追加修正（同ブランチ内・`PromptVersion` 〜fact-grounding-8）

- **v2.1.4（Web検索の事実ベース化）を本ブランチへマージ**: s12（駐車場で「社内文書に記載がない」とWebテンプレ誤回答）・s13系の捏造がmain既存欠陥であることをベースライン実測で帰属確定のうえ取り込み
- **混同禁止規則を独立文に復元**（fact-grounding-2）: v2.1.4が規則を1文に圧縮合并したことで「長期出張の承認者」問が決定的に所属長/部長を混同する回帰（quick/standard両階級で確認）が発生。旧来の2文構造に戻し解消
- **規則⑤追加**（-3）: Web検索OFF時に【参照情報】が存在しないのに「参照情報によると晴天」等の外部状況を捏造する揺らぎ（s13）と、ニュース捏造（t62）を解消。実測5回連続で正直な拒否回答
- **規則⑥（前提ガード）＋言い換え許容句追加**（-8）: 「〜できますか／〜する方法は？」で規定に明記が無い手続き・許可をでっち上げる捏造（t76: 個人旅行を「経費可能」と誤答）を解消。言い換え許容句（「公差」の問いに「±0.2」表記で回答可）により意味的近傍の正答（d09）を維持
- **規則⑦（対象置換防御）は試行後に撤回**: 効果が薄い一方、規則リスト肥大で小モデルの注意力が希釈し複数事実回答の網羅性が劣化（s09/s11/d09）。**プロンプト規則の追加は18問セット＋図面10問の両ゲートで検証すべき**という知見を確立
- シナリオ期待値更新: t61/t73はv2.1.4の正直拒否文言に合わせて更新（t62/t74は捏造なので厳格なまま）
- 検証結果: 単体**88/88**（56→88）／図面E2E **10/10**／18問 16/18（不合格はmain既存の揺らぎs07/s12のみ）／118問フル **104/118**（mainベースライン103/118・本ブランチ固有の失敗はゼロ、t25は改善）
- 既知事項: t74（QA文書の「対象置換」: パスワード再発行→健康保険証再発行）はプロンプト耐性（両階級同一誤答）→**生成後バリデーションガードをbacklog**。`year-end-policy.pdf` 取り込み失敗は375バイトの破損スタブで旧来同一挙動（非回帰）

## 2026-09-20（v2.1.4・Web検索の事実ベース化 — 検索結果と回答の一致）

### v2.1.4 — 検索スニペットだけで答えさせず、ページ本文を取得してから答えさせる

- **Web検索回答の作話（事実と逆の回答）を修正**: 実機で「神奈川県の今後の一週間の天気を教えて」（Web検索ON・quick階級）に対し、DuckDuckGoの検索スニペットはサイト説明文（「2週間先までの天気がひと目でわかる…」等）で予報の事実を一切含まないにもかかわらず、モデルが「週初めは晴れが続く」という**事実と逆の予報を作話**する事故を確認（当日の実際は台風25号接近で雨・雷雨）。対処として**検索後に対象ページの本文を実際に取得してスニペットへ補強**（`WebSearch.EnrichAsync`）し、事実が参照情報に入った状態で回答させる一般機構を追加（ドメイン特化なし・取得は最大256KB・6秒タイムアウト）
- **ページ本文抽出の窓選択を位置スキャン化**（`WebSearch.BestWindow`）: クエリ語と最も重なる約120字の探査窓を特定し、その中で最も固有な語（全文での出現数が最少のクエリ語）の出現位置から約700字を抽出。見出し・ナビが長いページや表組み（文末記号が無く文分割できない）でも関連箇所の本文・表を拾い、冒頭のナビを巻き込まない。`{{item.xxx}}` 等 JS未レンダリングの雛形断片（SPA）は本文として採らない
- **補強対象を関連度順に1件選定**: SERP順位上位固定ではなく、タイトル・スニペット・URLとクエリの語一致が最も多い結果を700字で補強（順位上位がポータルトップ頁で、実際の中身は下位の個別頁というケースへの対応）。失敗時は次点を試す。ctx=2048の予算内に収めるため1件のみ
- **出典プレビューに実際の本文が見えるよう修正**: 補強スニペットは「本文抜粋を先頭、サイト説明文を後」の順に組み立て、UIが表示する先頭110字にページ実内容が出るようにした（従来はサイト説明文だけが表示され、回答と出典が食い違って見える一因）
- **プロンプト（ctx予算・足かせ）の調整**: 参照情報に書かれていない事実の創作禁止を番号付き規則として明記（①書かれた事実のみ答え書かれていない日付・数値・天気・名前を作らない ②数字がサイトごとに異なる場合は各サイトの数字をそのまま併記し新たな数字を作らない ③表の一部だけ読み取れた場合は読み取れた行のみ ④事実が無ければ「具体的な情報が得られませんでした」と明言）。Web参照が大きい場合は履歴注入を直近1往復に削減、プロンプト内のURL表記を省略、Web回答の生成上限を300→500トークンに緩和（14日分の予報列挙が途切れない）
- **LLMエンジンのctx上限を2048→4096に引き上げ**: Web本文抜粋を注入すると実測でプロンプト alone が約1560トークンに達し、2048では生成が十数トークンで途切れる（`truncated=1`）ため。KVキャッシュ増は軽微（4Bで数百MB程度）
- **Web由来の回答を回答キャッシュ対象から除外**: Web検索結果は時々刻々変わるスナップショットのため、古い（誤った）回答の恒久化を防止。あわせて`TimeSensitiveQuestion`に天気・気温・ニュース・株価・為替等の変動トピックを追加
- **プロンプト仕様変更に伴い回答キャッシュを全消去**（`PromptVersion` → `2026-09-20-fact-grounding`）: 今回毒化していた天気の誤回答キャッシュも起動時に自動無効化
- 実機検証（standard=Qwen3-4B・天気以外の4分野でも実測）: 天気「神奈川の週間予報」=抽出ページどおり10日分・参照にない市区町村は「確認できません」（捏造ゼロ）。**ニュース系**「台風25号の進路」=記事本文（位置・警戒情報）を忠実に要約し出典明示。**百科数値系**「日本の総人口」=Worldometer頁の実数値（1億2240万/国連1億2242万）をそのまま回答（頁HTML実在確認済み）。**ライブ数値系**「ドル円」=Yahoo!ファイナンスの実レート（156.85/156.93）を抽出し、正確な現在値は「参照にない」と正直に案内（捏造ゼロ）。**公式文書系**「Windows 10サポート終了日」=2025年10月14日（正しい・Microsoft公式頁から）。**quick階級（1.7B）は表の忠実な転写が依然不安定**（1地点の行を他都市に複製する等）なため、Web検索ONでの利用はstandard以上を推奨。ナレッジ質問（タクシー費上限）は従来どおりKB出典付き回答・回帰なし。単体テスト追加（窓選択・HTML除去・文字コード判定等）し全56件合格

## 2026-09-17（v2.1.3・システム日時による直接回答）

### v2.1.3 — 日付・時刻の質問をナレッジ・Web検索なしで回答

- **日付感応質問（「今日は何日」「今何時」等）は、ナレッジにもWeb検索にも該当がなくてもPCのシステム時計を根拠に直接回答**: 従来はWeb検索OFF＋ナレッジなしでは「該当する記載がありません」と返り、日付すら答えられなかった。空ガード・リランクガードの両拒否経路を日付感応質問では通過して生成まで進めるようにした
- **userメッセージにシステム日時（日付＋曜日＋時刻）を明示**（`Rag.SystemInfoLine()`）: system側の日付行が前置きキャッシュ効率で日単位固定なのに対し、こちらはリクエストごとの正確な時刻（HH:mm）を与える。「今何時？」にも対応。userメッセージは前置きキャッシュ対象外のためキャッシュ効率への影響なし
- **システムプロンプトに【システム情報】の取り扱い指示を追加**: 「日付・時刻・曜日の質問はシステム情報を根拠に正確に答える（社内文書・Web検索がなくてもよい）」
- **`--config` 指定起動時に既定config.jsonを生成しないよう修正**（`EnsureDefaultConfig`）: publish出力フォルダから `--config` 付きで検証起動した際に実行フォルダへ既定config.jsonが書き出され、それが意図せずインストーラに同梱されてインストール後のアプリが誤ったパス（存在しないmodels_dir等）を参照する事故を防止
- 実機検証（Web検索OFF・ナレッジ0件）: 「今日は何日ですか？」→「2026年9月17日は木曜日です。」（1.4秒）、「今何時ですか？」→「現在の日時は2026年9月17日（木曜日） 05:09です。」。一般質問の「該当なし」ガード・KB出典回答・通常キャッシュは従来どおり動作

## 2026-09-17（v2.1.2・日付質問の誤回答修正＋メッセージコピー）

### v2.1.2 — 現在日付のプロンプト注入とメッセージコピー機能

- **「今日は何日ですか」等の誤回答を修正**: 従来はプロンプトに現在日時が無く、Web検索スニペットにも日付が含まれないまま回答を生成させたため、モデルが「2025年10月15日」のような架空の日付を回答する事故が発生した。**システムプロンプトに現在日付＋曜日を注入**（`Rag.CurrentDateLine()`）し、日付・時期の質問に正確に答えるようにした。実機検証: 修正前「該当する記載がありません」→修正後「今日は2026年9月17日（木曜日）」
- **日付感応質問の回答キャッシュ除外**（`Rag.TimeSensitiveQuestion()`）: 「今日・今月・現在」等を含む質問はキャッシュを読み書きしない。翌日に古い日付が再生されるのを防止（通常のナレッジ質問のキャッシュは従来どおり有効）
- **プロンプト仕様変更に伴い回答キャッシュを全消去**（`PromptVersion` 更新）: 旧プロンプトで生成された誤回答（毒化キャッシュ）は次回起動時に自動無効化
- **メッセージカード時刻横にコピーアイコンを追加**: ユーザー発言は入力テキスト、**AI回答はMarkdownソース**（`**太字**`・箇条書き等の記法込み）をクリップボードへコピー。クリック後1.5秒間チェックアイコン＋緑表示で完了を知らせる（WebView2のClipboard API＋非対応環境向けフォールバック）
- **起動ウォームアップの前置きキャッシュを新プロンプト構成に一致**させ初回応答の短縮効果を維持（`Engines.WarmupAsync`）

## 2026-09-16（v2.1.1・アプリアイコン更新）

### v2.1.1 — 新ロゴへのアイコン統一

- **`assets/app.ico` を新ロゴ（緑グラデーション角丸＋白い吹き出し＋緑3ドット）に更新**: 旧icoは32px単一フレームだったのを、Storeロゴ `store-logo-1080.png` から生成した256×256 PNGフレーム1枚のシンプル構成に変更。エクスプローラ大きい表示・タスクバー等で従来粗かった表示が改善
- **適用範囲**: インストーラ/setup UI・アンインストーラ表示・デスクトップ/スタートメニューのショートカット（`{app}\assets\app.ico` を `ignoreversion` で上書き）・`ShineosQA.exe` 組み込みアイコン（`/win32icon`）。**v2.1.1へアップグレード インストールで全箇所が新アイコンになる**
- **Store申請アセット一式をリポジトリに追加**: 1:1 Box art `store-logo-1080.png`・2:3ポスター `store-poster-720x1080.png`・ヒーロー宣伝画像 `promo-hero.png`（詳細は v2.1.0 同日のコミット `8e80371`）
- **破損していた `store-logo-300.png` を修復**: d46b158 以降ノイズ化していたファイルを1080ロゴからの縮小版で再生成（MSIXパッケージ内ロゴ生成も修正）

## 2026-09-16（v2.1.0・Store提出用ライトインストーラ＋R2パッケージ配信）

### v2.1.0 — ライトインストーラ（モデル非同梱）構成の追加

- **インストーラの2構成化**: `installer-v2.iss` にモデル同梱を条件化。既定（フル版）は従来どおりモデル3点同梠の完全オフライン・約2.2GB。**`/DLiteBuild` で lite版（約47MB・モデル非同梠）**をビルド。CI（release.yml）は両方を生成し、lite版をActions成果物にも添付
- **背景: Store提出のパッケージサイズ上限**。Microsoft StoreのURL形式EXE/MSI提出はパッケージサイズ上限（約2GB）でフル版2.23GBが「パッケージ サイズの制限を超えました」エラーで拒否されるため、lite版をStore提出用とする。MSI化も検証済みで不可（WiXでビルドした2.24GBのMSIに「Windows Installerは2GB超パッケージ非対応」警告 WIX1158）
- **lite版の初回起動フローは既存実装をそのまま使用**（アプリ本体の変更なし）: チャットモデル未導入時 `needs_wizard=true` → 初回ウィザードで必須埋め込み→選択チャットモデル→リランカの順に約2.3GBをDL（SHA256検証・ミラー代替・再開対応）。実機検証: liteアップグレード→models退避でウィザード発火→埋め込みモデル634,553,760バイトをDL→SHA256カタログ値と一致→モデル復帰で `needs_wizard=false`
- **パッケージ配信（Cloudflare R2）**: バケット `shineos-downloads`（r2.dev公開URL）に lite版 `ShineosQA-Setup-2.1.0-lite.exe`（v2.1.0タグのCI公式ビルド・SHA256 `418fea23…`・46,760,479バイト）を配置。大容量フル版は「Cron駆動Worker + R2バインディング」で配置可能（手順: [docs/r2-upload-worker.md](docs/r2-upload-worker.md)）
- **ドキュメント整備**: [docs/store-submission.md](docs/store-submission.md) をStore公開マスタードキュメントとして全面改訂（lite版フォーム値・日英listing文・審査メモに初回DLを明記・サイレントパラメーター・リターンコード⇔標準シナリオ対応・終了コード説明ページ https://pub-cbe981f96fcc423d8c28124ab0fccba5.r2.dev/error-codes.html）。README両言語と PRIVACY.md の通信タイミングを lite版挙動に更新

## 2026-09-15（v2.0.1・インストール直後のナレッジ登録案内）

### v2.0.1 — ナレッジ登録案内ダイアログ

- **インストール（更新）直後の初回起動に「ナレッジを登録しましょう」案内を表示**: 社内文書の登録方法（PDF・Word・Markdown・テキスト・ドラッグ＆ドロップ対応）を1回だけ案内。検知は `{app}\install.completed` の更新時刻で「新しいインストール」を判定するため、**再インストールやバージョン更新のたびに再度表示**され、既存ユーザーの `%APPDATA%` フラグが理由で出なくなる従来の不具合（v2.0.0で案内が出なかった原因）を解消
- **「次回から表示しない」チェックボックス**（既定ON）: チェックしたままボタンを押すと今回のインストールでは以後表示しない。チェックを外せば毎回表示
- **「ナレッジ登録へ進む」ボタン**: ダイアログを閉じてWeb UIのナレッジタブを直接開く（WebViewロード前ならロード完了後に自動遷移）。「あとで」ボタンは閉じるだけ
- 実機検証: 新規インストール→起動時に案内表示→「進む」でナレッジ管理画面へ遷移→フラグ記録→**再起動で非表示**を確認

## 2026-09-14（v2.0.0・アーキテクチャ再設計）

### v2.0.0 — llama.cpp 直接実行・自作軽量バックエンドへの全面再設計

v1（Ollama + Open WebUI）を廃止し、**llama.cpp（llama-server）を直接実行する構成**に再設計。Python・venv・Webサービス常駐・ソースパッチがすべて消え、低スペックPC（8GB RAM・GPUなし）での高速応答と「閉じたら完全停止」を実現した。

- **AIエンジン**: Ollama → **llama.cpp 公式プリビルド llama-server.exe**（CPU版・仲介層なし）。チャット／埋め込み／リランクの3プロセスをバックエンドがジョブオブジェクトで管理（クローズ時に孤児プロセスなし）
- **アプリ層**: Open WebUI + Python + 5種のソースパッチ → **自作C#バックエンド1実行ファイル**（SQLite + FTS5 内蔵・静的Web UI 配信）
- **UI**: TypeScript + Vite 製チャットUI を WebView2 シェルで表示（URL入力不要・起動約2秒）
- **3階級モデル**: ⚡クイック（Qwen3-1.7B IQ4_XS・**インストーラ同梱**）／🎯標準（4B IQ4_XS）／🏆高品質（30B Q3_K_XL）。入力欄のモデルボタンで即切替、未DLモデルは同画面からDL（スピナー・進捗％表示・二重クリック防止）
- **高速化**: 起動時モデル事前ロード＋推論ウォームアップ、プロンプト前方固定化（プリフィックスキャッシュ）、高信頼時リランク省略、回答キャッシュ、IQ4_XS軽量子化（生成+8〜15%・サイズ-9%・品質劣化なしを18シナリオで実証）
- **RAG強化**: bge-reranker-v2-m3 リランク、隣接チャンク連結（文脈途切れ解消）、回答ガード（-5.0）維持
- **プロセス・ポート完全停止**: アプリ終了時にバックエンド・llama-server・ポート8300-8303をすべて解放（実機検証済み）
- **インストーラ**: 管理者権限不要（%LOCALAPPDATA%配置）、サイレントインストール対応、初期モデル同梱で**インストール即利用可**（DL不要）
- **モデルDLの安全性**: HuggingFace から GGUF を直接DL・SHA256検証・中断再開対応
- **OSS・モデルライセンス整備**: THIRD-PARTY-NOTICES.txt を v2 構成に書き換え（llama.cpp=MIT、Qwen3=Apache 2.0、bge-m3=MIT、bge-reranker-v2-m3=Apache 2.0、SQLite系）。ライセンス全文8点を `vendor/licenses/` に格納しインストーラが `{app}\licenses` へ同梱。PRIVACY.md・CODE_SIGNING.md を v2 実態（オフラインインストーラ・%LOCALAPPDATA%配置・ポート8300）に更新
- **終了コードの一意化（Store申請対応）**: インストーラに独自終了コード機構を追加 — 同一バージョン完了済みサイレント再実行=**11**、ディスク空き容量不足=**12**（v1.0.76と同じ意味を維持。実機検証: 0/11）。バックエンドに終了コードを実装 — config.json破損=**20**、ポートバインド失敗=**21**、knowledge.db破損=**22**、想定外例外=**30**（実機検証: 20/21）。全終了コードが単一名前空間で重複しないよう番号帯を分離し [error-codes-v2.md](docs/error-codes-v2.md) を実装実態に全面改訂
- **アンインストール時のデータ取り扱い**: 対話時はナレッジ・チャット履歴（`data`）の削除確認を表示。サイレント時はデータを残す（本体は完全削除）
- **モデルメニューのアイコン修正**: 標準モデルの行アイコンが同心円（読み込み中スピナーと紛らわしい）だったのを十字線付き照準型に変更。送信フォームのモデルボタン内アイコンも選択階級（⚡/🎯/🏆）に連動して切り替わるように
- **UIからバージョン表記（v2）を削除**: ヘッダーのブランド名は「📖 社内知恵袋」のみに（ユーザーには無関係な情報のため。READMEのスクリーンショット4枚も新しい画面で撮り直し）
- **ヘッダーに「powered by Shineos」リンクを追加**: ブランド名の下に灰色の小文字で表示し、クリックすると既定のブラウザで https://shineos.com/ を開く（開けるURLは公式サイトの定数のみに制限。READMEのスクリーンショット4枚も再撮影）
- **テスト資産の全面拡充**:
  - **単体テスト（xUnit・32件）**: `app/ShineosQA.Backend.Tests/` を新設。チャンカー（分割・オーバーラップ）/コサイン/関連文スニペット（クロスリンガル全文返却仕様）/閾値の不変条件/Db（設定・answer_cacheマイグレーション・パラメータバインディング）/ChunkIndex（ハイブリッド検索・隣接チャンク・ファイル削除）/Config解析（パス穿越拒否・破損JSON・未知キー無視）— 32/32合格
  - **Golden QA 118問**: `scenarios-100.json`（正答60・拒否20・表記ゆれ10・横断10＋旧18）。quick 1.7Bで **108/118（91.5%）**。失敗10件を条件混同・生成劣化・逆質問/言い換え検索ミス・罠ガード抜け・横断recallに分類し RESULTS.md に記録
  - **UIスモーク自動化**: `spikes/phase0/ui-smoke/ui-smoke.ps1`（実ウィンドウWin32操作で起動→タブ切替→メニュー開閉、7/7合格）
  - **インストーラ失敗系**: `spikes/phase0/installer-tests.ps1` — アップグレード2.0.0→2.0.1（exit=0・データ17チャンク保持）、モデル破損（エンジンロード失敗をログ記録しつつバックエンド生存・自動復元）を実機検証
- **ナレッジ一括取り込みの頑健化**: `/api/knowledge/import` が1ファイルの解析失敗（破損PDF等）でフォルダ全体の取り込みを500で中止する問題を修正。失敗ファイルをスキップし `{"imported":N,"failed":[...]}` を返す（実機で破損PDF混入フォルダを検証: 5成功+1優雅な失敗）
- **モデル破損時の無応答を本質解消（240秒→2.5秒）**: 破損GGUFでllama-serverが瞬死する際、①ヘルス待ちが120秒満値待ち、②OOM誤診による同一ファイル再試行で計240秒無応答、という2層の欠陥を特定。**起動前SHA256整合性検証**（`ModelIntegrity`・永続キャッシュ `{data}/model-verify.txt` で再ハッシュ回避）＋**ヘルス待ちの瞬死検知**＋**OOM誤診の阻止**（SHINE_E_MODEL_HASHはフォールバックせず即エラー）＋**サーキットブレーカー**（連続3回失敗で即時エラー、モデル再DL完了・階級切替でリセット）＋SSE `SHINE_E_MODEL_HASH` エラー（「設定画面から再ダウンロードしてください」）を実装。実機検証: 破損モデルで2,543msでエラー応答、復元で通常動作復帰、起動時検証コスト計約4秒（1回のみ）。単体テスト6件追加（計38件）
- **カスタムインストール先（日本語・スペース込み）に対応**: インストーラがconfig.jsonをInnoの`SaveStringToFile`（ANSI書き出し）で生成していたため、日本語パスにインストールするとconfigが文字化けし、バックエンドが化けたパス（U+FFFD）で**ゴミディレクトリを生成してエンジン・モデルが全停止**する障害を発見・修正。config.jsonの生成をバックエンド初回起動時に移管（UTF-8・絶対パス・`EnsureDefaultConfig`）。インストーラはASCIIの完了マーカーのみ書く。バックエンド側にもU+FFFD入力configのフェイルファスト検知（終了コード20）を追加。実機検証: 「D:\テスト場所 ShineosQA」へインストール→configがUTF-8で正常生成・3エンジン稼働・ガード質問までE2E動作
- **モデル破損の「再ダウンロード」導線を完了**（RESULTS.md 第11ラウンド）: ①`InstallAsync`が既存ファイルをSHA照合し、破損時は削除して強制再DL（従来は`File.Exists`で無音スキップ＝案内が袋小路）②`/api/models`に`corrupted`フラグ（検証キャッシュ参照）③モデルメニュー・設定画面に「破損」バッジ＋「再ダウンロード」ボタン。実機で完全旅程を検証: 破損→2.6秒エラー→ブレーカー72ms→バッジ表示→ボタンで1GB自動再DL（167秒・SHA検証付き）→フラグ解除→チャット復旧
- **DL進捗APIの修復**: `/api/models/progress` が `DownloadProgress` のフィールド宣言のため常に `{}` を返していた（System.Text.Jsonはフィールドをシリアライズしない）。プロパティ化し回帰テスト追加
- **破損テストの決定化**: d2シナリオに回答キャッシュクリアを追加（キャッシュはLLM起動前に評価されるため、同一質問の過去の正答が破損検証を回避していた）。単体テスト計42件
- **他アプリへの影響を抑制（他アプリ優先モード・既定ON）**: LLM生成の優先度をNormalからBelowNormalに変更し、設定画面にトグルを追加（速度優先に戻せる・変更は即時反映）。**実測（Ryzen 7 5700U・8C/16T・16並列の重処理と併走）**: 従来（Normal）は他アプリのスループットが-11%低下していたのに対し、本モードで**低下なし（ベースライン比105%）**、生成速度も劣化なし（39.7秒 vs 41.3秒）
- **アイドル時のメモリ自動解放を拡張**: 従来はLLMのみ（60分後）だったのを、**rank（LLM解放の5分後）→ embed（同60分後）も段階的に解放**し、アプリを開いたままの常駐メモリを2.29GB→約0.08GB（バックエンドのみ）まで削減。次の質問で自動再起動（実測18秒で回答まで復帰）
- **config.jsonのパース漏れを修正**: `idle_unload_minutes` / `bg_friendly` / `threads` がJSONに書かれても読み取られず黙って既定値になる問題を修正（回帰テスト追加、単体テスト計43件）
- **Microsoft Store申請準備**: バージョン表記を `2.0.0-dev` → `2.0.0` に修正。申請ドシエ `docs/store-submission.md`（申請フォーム転記用のパッケージ情報・リターンコード表・Store掲載文・年齢区分回答例・画像アセット一覧・残課題チェックリスト）とStoreロゴ300px（`assets/store-logo-300.png`）を追加。残る外部作業はSignPath署名・プライバシーポリシーURL公開・Partner Center登録のみ
- **レイテンシ改善余地の定量検証**（RESULTS.md 第12ラウンド）: KV量子化+`--cache-reuse`（8問全て悪化→却下）、スニペット圧縮160/180字（TTFB中央値-8%だがGolden QA 118問が108→101に劣化→却下・240字が最適点と判明）。**現状の構成（1.7B IQ4_XS・ウォーム中央値約4秒・同一質問0.2秒）が品質を維持した実用的な最適点**と結論。全実験の生データを記録
- **インストーラ**: バージョンを `ISCC /DMyAppVersion=` で上書き可能に（アップグレード経路テスト用）

### v1.0.78 — ナレッジ即時反映・障害調査性向上・XXE防御の強化（v1系最終版）

- **ナレッジ即時反映パッチ（fresh knowledge lookup）**: チャット時に参照するナレッジ紐付けが app.state.MODELS のスナップショットのため、UI・API からナレッジを追加・解除してもサービス再起動まで反映されない問題（実機検証）を修正。チャット毎に DB から最新の紐付けを取得して上書きする
- **install.log の網羅性**: configure_model・setup_knowledge・スモークテストの出力を install.log に統合し、末尾に `=== POST-INSTALL COMPLETED ===` マーカーを追加。install.log 単体でインストール全体の成否を判別できる
- **XXE 防御の強化（defusedxml ゲート）**: PPTX 内 XML の解析前検査に defusedxml を追加（DTD・実体参照を含む XML を全文走査で拒否。defusedxml 未導入環境では従来の安全パーサで防御）。defusedxml を依存関係に追加
- ドキュメント: インストール所要時間の表記を並列化後の実測値（約15〜40分）に更新（README / README-EN / ユーザーマニュアル）

### v1.0.77 — インストールの高速化（並列化・スモーク短縮・Defender一時除外）

- **並列インストール（v1.0.77）**: 従来直列だった Python → Ollama → モデルDL → Open WebUI を、依存関係ごとに2フェーズへ再構成し並列実行（Phase 1: Python ∥ Ollama / Phase 2: モデルDL ∥ venv+pip）。実測の全体時間（約48分）に対し 10〜20分程度の短縮を見込む。進捗バーは2ジョブの進捗を合成した％を表示
- **インストール時スモークテストを短縮（--quick）**: 資料作成テスト（PDF/PPTX/DOCX・約3分）をインストール時の検証から除外（リリース前検証は従来どおり実施）。パッチ適用・ナレッジ網羅性・RAG回答・ガードレール等の核心検証は残す（約1分）
- **`/SKIPVERIFY=1` パラメータ**: 無人展開時に導入検証を省略可能に（スキップは install.log に記録）
- **Defender 一時除外タスク（既定OFF）**: ウィザードのチェックボックス（または `/TASKS="defender"`）で有効化すると、インストール中のみアプリフォルダ等を検査対象外にし、完了時に自動で解除。venv/pip の大量ファイル展開が高速化される場合がある

### v1.0.76 — インストーラの終了コードをシナリオ別に一意化（Microsoft Store 申請対応）

- **カスタム終了コード機構を追加**: Store のインストールクライアントがシナリオを区別できるよう、独自コード（11/12/13）を返す仕組みを実装（DeinitializeSetup で ExitProcess を呼び出す方式。0 の場合は Inno 標準コードのまま）
- **ディスク領域不足 = 12**: preflight.ps1 に空き容量チェックを追加（システムドライブと Program Files 配下のドライブ・15GB 未満で検出し ini 経由で報告）。セットアップが 12 で停止する
- **ネットワークエラー = 13**: Python ランタイム・Ollama 本体・AIモデルのダウンロード失敗を setup_python / setup_ollama / setup_openwebui が 13 で区別して返す（run_all.ps1 が伝播し、セットアップが 13 で終了）
- **既に存在 = 11**: 同一バージョンが完了済み（install.completed マーカーあり）の環境への `/VERYSILENT` 再実行時に、再インストールを行わず 11 で即座に終了。対話実行時はモデル再選択・修復のため従来どおり実行される（失敗途中の再開もマーカーなしのため影響なし）
- **パッケージ拒否 = 14（参考値）**: ポリシーブロックはプロセス起動前のため本 EXE からは返らないが、対応表の一意性のため割り当て
- 終了コードの一覧とシナリオ対応表を docs/exit-codes.md に整理（README からリンク）

### v1.0.75 — ナレッジ登録のルートファイル取りこぼし回帰を修正＋ガードレールの構造的強化

- **setup_knowledge.ps1 がサブフォルダ存在時にルート直下の文書（QA_list.md）を登録しない回帰を修正**: v1.0.74 で同梱ナレッジに `knowledge/社内規程/` を追加した結果、新規インストールでサンプルQA（経費精算の手順・パスワード内線番号・在宅勤務・PC貸出）が欠落し、パスワード質問に根拠のない創作回答が返る状態だった（v1.0.74 での新規インストール実機で再現・確認）
- ルート直下の文書はサブフォルダの有無に関係なく「社内ナレッジ」コレクションへ登録する方式に変更（サブフォルダのコレクション化は従来どおり。対象ファイル0件のコレクション空作成も回避）
- **setup_knowledge.ps1 の file/add 間欠失敗にリトライを追加**: アップロード直後のコレクション追加は 400 で間欠失敗し、失敗のまま進むとその文書がナレッジに登録されず静かに欠落する（実機で再現・確認。リトライ 3 回で解消することも確認）
- **検索ノイズの構造的フィルタ（RELEVANCE_THRESHOLD=0.55）**: ナレッジ増加時に無関係な文書断片が回答に混入する問題（寮の質問に在宅勤務の内容で回答等）に対し、関連性しきい値を導入。実測（bge-m3 コサイン類似度）では無関係断片は最大0.52、正答断片は最小0.58で分離するため 0.55 として全実答質問の正答を維持したまま無関係断片のみを除去
- **no-match guard パッチ**: しきい値で全断片がフィルタされた場合、従来は RAG テンプレート（対象外ルール付き）ごと省略され、素の質問だけを受け取った小規模モデルが寮の手続き等を創作する問題を確認。該当文書が無いことを明示的に資料欄へ注入し、対象外ルールを確実に発動させる（実機で拒否率 1/6 → 5/5）
- **温度 0.2 → 0（貪欲デコーディング）**: 同一質問で回答が揺れる問題（可否質問の対象外混入・固有名詞の脱落など）を実機で解消。技術ノートの「Q&A用途では決定論的な回答が望ましい」方針に整合。応答時間も短縮（例: ガードレールチェック 22秒 → 8秒）
- **スモークテスト強化**: ナレッジ網羅性チェックを既定項目へ追加（「PCの貸出申請は？」→ 窓口3階。QA_list 固有の固有名詞でナレッジ取りこぼしを検知。デフォルトの15,000円チェックは v1.0.74 の回帰をすり抜けたため）。`--eval` の代表質問セットも同様に更新
- setup_knowledge.ps1: サービス稼働中に再実行した場合、新規コレクションの検索への反映には再起動が必要な旨をログへ案内（稼働中サービスへの紐付けは再起動まで反映されない実測に基づく）。ユーザーマニュアルにも追記
- CI（release.yml）に Python 構文チェック（compileall）を追加
- 実機検証: スモークテスト全項目（既定+代表質問セット17項目）を2回連続で全合格

## 2026-08-31

### v1.0.74 — 保守ツールの実在化・履歴遅延対策・検証強化

- **存在しない「ShineosQA-repair.bat」を案内していた問題を修正**: パッチ健全性チェックの警告文が実在しないファイルを指していたため、`scripts/repair.bat`（管理者昇格付き）を新設し、インストーラがデスクトップに「ShineosQA-repair」ショートカットを作成するようにした（案内文も実在する名称に統一）
- **repair_middleware.py のハードコードパスを修正**: インストール先をスクリプト位置から自動検出するようにした（従来は `C:\Program Files\ShineosQA` 固定で、カスタムインストール先では修復不能だった）。パッチ実行を runpy 化し、未配置スクリプトは警告スキップするようにした
- **履歴遅延対策**: 長い会話でプリフィルが膨らむ問題（実測: 10往復で初回トークン+7.6秒）に対し、Ollama 送信ペイロードのみ直近6往復分に打ち切るパッチ（`patch_openwebui_hist.py`）を追加（UI表示・保存履歴は無変更）
- **スモークテスト強化**: 埋め込み遅延チェックの追加と `--eval` モード（代表質問セット10ケース: 手順/可否/固有名詞/複合/数値/条件付き可否/期間/ポリシー。3Bモデルの回答揺らぎ対策としてケース単位の複数試行に対応）
- **プライバシーポリシー全面改訂**（PRIVACY.md）: 実挙動（インストール時通信・Web検索ON時のDuckDuckGo送信・保存先・削除方法）を正確に文書化し、README に「プライバシー」セクションを新設
- **サンプルナレッジ追加**: `knowledge/社内規程/`（出張・旅費規程/慶弔休暇規程/セキュリティ規程）と対応する評価シナリオを追加（実機で全シナリオ正答を確認）
- file_export_mcp.py の `\W` エスケープ警告を修正（raw文字列化）
- 技術資料の検索設定記載を実値（チャンク300/オーバーラップ30）に修正

## 2026-08-30

### v1.0.73 — 複合質問時に対象外文言が末尾に混入する問題を修正
- 3Bモデルが複数の参考断片で回答できた場合でも、プロンプト内の対象外文言を「おまけ」で末尾にコピーする問題に対処
- システムプロンプトのルール3とRAGテンプレートに出力条件を明記: 「資料の一部でも答えられる記載があれば対象外の文言は絶対に出力せず、対象外の文言は回答できる記載が一つも無いときだけ使う」
- 実機検証: 旅費申請の質問x3で末尾混入なし（修正前は3回中1回発生）

### v1.0.72 — 空クエリ対策パッチの適用漏れ修正 + 天気ハルシネーション対策
- patch_openwebui_rag.py の ANCHOR3 インデント不一致により、空クエリ対策パッチが v1.0.48 から一度も適用されていなかった問題を修正（ラッパーの起動時自己診断が誤警告を出す原因だった）
- RAGテンプレートに反幻覚ルールを追加: 天気・気温・降水確率など参照資料に数値や事実が確認できない場合は、雛形の形式（-℃ や % など）を使わず対象外の文言のみ回答する
- 実機検証: 天気質問x2とも実データ回答（ハルシネーションなし）

### v1.0.71 — アンインストーラの無効定数 appdata を修正（userappdata）+ Ollama ログ削除
- v1.0.70 の修正後も appdata 定数が無効（Unknown constant）でアンインストールが中断する問題を修正（正しくは userappdata）
- RemoveOllamaCompletely に localappdata\Ollama（ログ等）の削除を追加
- コード部の全 ExpandConstant 定数を検証（無効定数は残存なし）

### v1.0.70 — アンインストール時の定数エラーを修正
- アンインストール時の Ollama 完全削除処理で無効な Inno Setup 定数 {userprofile} を ExpandConstant しており、Runtime error (Unknown constant) でアンインストールが中断していた問題を修正（環境変数形式 {%USERPROFILE} を使用）
- Pascal コメント（波括弧形式）内に別の波括弧 {%USERPROFILE} を書くとコメント終端として解釈され構文エラーになる問題を修正（コメントから波括弧を除去）

## 2026-08-29

### v1.0.69 — 資料作成のコマンド別形式対応（/pdf・/pptx・/docx）
- ユーザー提案により形式トークン方式（/doc pptx 〜）からコマンド別方式（/pdf・/pptx・/docx + /doc は PDF 既定の別名）に変更
- PPTX 出力: python-pptx（表題スライド+セクションごとに1枚・箇条書き）/ DOCX 出力: python-docx（表題+目次+見出し+本文）
- 保存期間管理を3形式へ拡張（7日自動削除）、smoke_test に /pdf・/pptx・/docx テストを追加
- 実機テスト: 9/9 合格 + 3形式のファイル検証（PDF 2頁/PPTX 5枚/DOCX 33段落）

### v1.0.68 — 資料作成をLLMバイパス方式に刷新（/doc コマンドに変更）
- 従来の案内文省略・脚色問題の根本原因（最終応答を3Bモデルに復唱させていた構造）を解消: main.py の process_chat 先頭で /doc を検出し、パイプラインの案内文を OpenAI 互換 SSE に変換して標準経路で配信。最終応答から LLM を排除
- PDF URL は delta.annotations (url_citation) で「ソース」カードにも表示、生成 PDF の7日自動削除を追加
- トリガーを /資料 から /doc に変更、実機テスト 9/9 合格

### v1.0.67 — 資料作成の応答文を短文化（小モデルの復唱省略・脚色に対応）
- 3Bモデルが長文の案内文を省略・脚色して伝わらない/目次に合わせた虚の手順を生成する問題に対処: チャット文は「作成完了+PDF URL+注意書き」の短文に変更し、目次はPDF側に移動
- PDFに目次を追加、実機テスト 9/9 合格

### v1.0.66 — /資料 コマンドでナレッジからPDFを自動生成する機能を追加
- tools/doc_pipeline.py を新設: 目次生成（Ollama構造化出力でJSON強制）→ セクションごとにナレッジ検索+執筆（ループバック呼び出しで既存RAGを再利用）→ reportlabでPDF生成 → data/mcpo_output に書き出し
- 小モデル前提の設計（判断・検索・ファイル化はすべてコードが実行）、patch_openwebui_doc.py を新設

### v1.0.65 — Web検索が動作しない問題を修正（5つの不具合を解消）
- クエリ生成タスクを無効化（3Bモデルが {"queries": []} を返し「No search query generated」で無反応になるため。質問文をそのまま検索クエリに使用）
- middleware.py に空クエリ時フォールバックパッチを追加（防御壁）
- WEB_LOADER_CONCURRENT_REQUESTS を明示設定（未保存の null が asyncio.Semaphore(null) で TypeError になるため）
- WEB_SEARCH_RESULT_COUNT=3 を設定（無制限だと埋め込み化に6分かかるため）
- aiodns を除去（c-ares が DNS 解決に失敗しページ取得が全滅する環境対策）
- モデルの capabilities.web_search=true を設定（トグル表示条件）
- 実機検証: ON時=実天気データを複数ソース付きで回答、OFF時=ナレッジから回答

### v1.0.64 — Web検索のWebページ取得エラー修正
- v1.0.63 の Web検索で「検索に失敗する」問題を修正: 検索結果のWebページ取得時に、未設定の `WEB_FETCH_MAX_CONTENT_LENGTH`（None）による型エラーが発生していた
- インストール時に `WEB_FETCH_MAX_CONTENT_LENGTH=1000000`（実質無制限）と `WEB_LOADER_TIMEOUT=30秒` を自動設定
- 検索エンジン設定（duckduckgo）もインストール時に自動設定するよう修正

### v1.0.63 — 対象外質問の案内改善＋Web検索ボタンの有効化
- ナレッジ対象外の質問（「今日の天気は？」など）への回答を改善: 「ナレッジに該当する記載がありません。気象課へ…」（部署名の捏造あり）→ **「この質問は社内文書の対象外です。一般的な情報はチャット入力欄のWeb検索をONにすると回答できます。」** に統一（実機確認済み）
- システムプロンプトの該当なしルールを修正: 実在しない部署名を生成させないルールを明記
- **Web検索ボタンを有効化**（従来は機能自体が無効でボタン非表示）: DuckDuckGo（APIキー不要）。既定はOFF（ユーザーがチャットごとにON/OFF選択）。ONにすると質問内容が外部の検索サービスに送信される点はマニュアルに記載済み

### v1.0.62 — 保守ツールのインストール先自動対応
- purge_orphan_vectors.py（削除済み文書の索引残骸掃除ツール）のベクトルDBパスをインストール先から自動検出するよう修正（scripts の親ディレクトリ/data/vector_db、見つからない場合は既定のインストール先へフォールバック）。インストール先を変更した環境でも --path 指定なしで掃除ツールが動作
- ユーザーマニュアルに追記: ナレッジに登録できる形式（PPTX含む）・画像ファイルは登録不可・文書削除後の索引残骸の掃除手順
- リリースノート（v1.0.61）の本文を整備

### v1.0.61 — PPTXナレッジ対応・画像ファイルのわかりやすい拒否・単位文字化け修正
- PPTX がナレッジに登録できない問題を修正: Open WebUI 0.11 は PPTX 読込に unstructured パッケージ（本製品は同梱しない）を必要とするため、python-pptx ベースの内蔵ローダーを直接使うようパッチ（実機で PPTX の登録・検索・回答を確認）
- 画像ファイル（PNG/JPEG等）をナレッジに添付したとき、英語の技術的エラーで失敗していたのを、**日本語でわかりやすく拒否**するよう修正（「画像ファイルはナレッジに登録できません…」）
- 削除済み文書の検索索引残骸に対応する保守ツール scripts/purge_orphan_vectors.py を追加（実機で残骸101件の削除を確認）
- 回答の単位文字化け（ヶ月→ヌ月）を RAGテンプレートの「か月」表記ルールで修正（実機22問で化け0件）
- セキュリティ強化（静的解析 高危15件を解消）: file_export のパス解釈脱出対策・外部画像取得廃止・ラッパーを sc.exe/netstat.exe 起動から ServiceController／IP Helper API へ移行・patch スクリプトの root 検証

### v1.0.60 — 回答の単位文字化け（ヶ月→ヌ月）の修正＋セキュリティ強化
- 負荷検証（33ファイル・30問）で発見: 3Bモデルが「ヶ」を出力できず「ヌ月」「ニ月」等と書き間違えることがある（数値自体は30/30すべて正確）
- RAGテンプレートに出力ルールを追加し、期間の単位は「ヶ月」を避けて「か月」と書くよう指示（例: 18か月。同じ意味の正しい表記）
- 実機検証: 同一質問22問で 単位化け 18問（82%）→ **0問（0%）**。数値正解 100% を維持
- 大量ドキュメント検証（MD/PDF/Word 各10本＋1万字/5万字/10万字の大型文書）も実施し、いずれも 30/30 問正解（10万字の文書でも位置によらず100%取得できることを確認）
- セキュリティ強化（静的解析で指摘された項目に対応）:
  - ファイル生成サーバー（file_export_mcp.py）のパス解釈脱出対策: LLM/ユーザー由来のファイル名をサニタイズし、出力先が必ずエクスポートフォルダ内に収まるよう保証
  - オフライン設計に整合させるため、外部画像検索API（Unsplash/Pexels）とリモート画像のダウンロード機能を廃止（SSRF対策・ローカルSD生成とローカル画像ファイルは引き続き利用可）
- 保守ツール追加: scripts/purge_orphan_vectors.py（ナレッジから削除した文書の検索索引残骸を掃除。実機で残骸101件の削除を確認）

### v1.0.59 — インストール中断の修正（ログ書き込み競合）
- アップグレード時に「STEP FAILED: Open WebUI (exit 1)」で中断されることがある不具合を修正（v1.0.58 実機で発生）:
  - 原因: インストーラ画面のログ末尾表示（毎秒 install.log を読み取り）が排他ロードとなり、PowerShell スクリプト側のログ書き込みと衝突。ログ書き込み失敗がそのままステップ失敗になっていた
  - 対策(1): 全スクリプトの Log 関数を非致命化。書き込みは短いリトライを行い、それでも失敗してもログを捨ててインストールを続行する（ログは手段であり、失敗で処理を止めない）
  - 対策(2): ログ末尾の読み取りは一度テンポラリへコピーしてから行い、衝突を構造的に排除。表示頻度も毎秒→3秒に1回へ緩和
- v1.0.58 自体の GPU/CPU 自動判定チューニングに問題なし（本件は v1.0.54 から潜んでいた表示機能との競合）

### v1.0.58 — GPU / CPU の自動判定チューニング
- インストール時に GPU を自動検出し、機種ごとに最適な動作モードを選択（ollama_common.ps1）:
  - **NVIDIA GPU 搭載機**: Ollama が自動で GPU オフロード（高速）。CPU 優先度の引き下げを適用せず速度を最優先。コンテキストサイズ（num_ctx）は KV キャッシュを VRAM に載せられるため 8192 固定
  - **AMD 専用GPU 搭載機**: 対応カードなら Ollama が自動で ROCm オフロード（対応外は自動で CPU フォールバック）。他アプリ保護の設定は CPU 機と共通
  - **内蔵GPUのみ / GPU なし**: 従来どおり CPU 推論＋他アプリ優先の各種制御（CPU 優先度「通常以下」等）
- 検出結果はインストールログと `{app}\gpu_mode.txt` に記録（サポート時の切り分け用）
- 実機検証: 本機（AMD内蔵GPU 0.5GB・RAM 15GB）は `gpu=cpu / num_ctx=4096` と判定され、ナレッジ紐付け・RAG テンプレート含め冪等に再設定されることを確認
- 実測参考: CPU 推論機（3b モデル）の応答は約 4〜15 秒。GPU 搭載機では大幅に短縮される

### v1.0.57 — 前回セッションの残骸を掃除
- アプリ起動時に、強制終了等で閉じる処理が走らなかった残骸を自動対処:
  - (1) サービス停止中なのに WebUI（venv 配下の python）が生きている場合に kill 試行（SYSTEM 権限で拒否された場合は従来のポート占有エラーで案内）
  - (2) サービス稼働中なのにポートの持ち主がサービス配下にない（WebUI ハング等）場合にサービス停止→起動で自動回復
- 実機検証: 強制終了残留状態からの起動で誤検知なくAttach・通常終了で完全停止を確認

### v1.0.56 — ヘッダー表示名の整理
- ヘッダー名の重複表記「社内知恵袋 powered by Open WebUI (Open WebUI)」を解消。Open WebUI が独自名に自動付加する "(Open WebUI)" をパッチ（patch_openwebui_branding.py）で無効化
- ヘッダーは1行目「社内知恵袋」＋2行目に小さめの「powered by Open WebUI」（帰属表示は維持）
- パッチ自己診断（check_patches）に名称表示パッチを追加

### v1.0.55 — 英語混入（RAGテンプレート漏洩）の修正
- 回答に英語の指示文（"### Task: Respond to the user query..."）と生の文書断片が混入する不具合を修正。原因は既定の英語RAGテンプレートを3Bモデルが出力にそのまま漏らすこと（実機検証）
- RAGテンプレートを日本語の簡潔版に差し替え（「日本語のみで回答・指示文や資料をそのまま貼らない・id がある資料は [1] 形式で引用・該当なし時は拒否」）
- 実機検証: 全質問で日本語のみ・テンプレート漏洩ゼロ・出典付き回答を確認

### v1.0.54 — 詳細ログをインストーラ画面内に表示
- インストール中の詳細ログ（STEP START/OK 等のリアルタイム出力）を、**別PowerShellコンソールではなくインストーラ画面内**に表示（進捗バー＋％/残り時間＋直近ログ6行を同一画面で毎秒更新）
- PowerShellコンソールは表示しない（「見覚えのない黒い画面」を出さない）
- 初回実装したカスタムフォーム（TSetupForm）は昇格実行時に "Resource TSetupForm not found" でクラッシュするため、実績のある標準プログレスページ方式に変更

## 2026-08-27

### v1.0.53 — インストール進捗の％・残り時間表示
- 長いインストール中に、インストーラ画面のプログレスバーに**進捗％・残り時間（見積り）・経過時間・現在の処理内容**を毎秒表示（ユーザーが中断してしまわないための対策）
- Ollama本体のダウンロードはファイルサイズ、AIモデルのダウンロードは ollama pull の％行を解析して、実測ベースの細かい進捗を表示
- 詳細ログのコンソール表示は従来どおり維持（インストーラ画面には要約を表示）
- run_all.ps1 とインストーラ間で進捗INI（UTF-16）を共有し、完了コードのハンドシェイクで確実に成否を判定（4時間の安全タイムアウト付き）

### v1.0.52 — 無料配布に向けた耐性強化
- アップグレード（再インストール）時、社内文書を自動で引き継ぎ（画面から追加した文書も退避・再登録）＋旧データを1世代バックアップ
- Open WebUI パッチの自己診断（アプリ起動時に適用状態を検査し、壊れていたら警告表示）
- 導入スモークテストの自動化（インストール完了後にRAG回答・ガードレール・モデル一覧・紐付けを自動検証）
- インストール画面に期待値（回答まで数秒〜20秒・規定Q&A特化）を明記
- 提供形態の明記（本体は無償・導入支援/保守サポートは有償）
- Open WebUI の帰属表示（ヘッダーに "powered by Open WebUI"）とサードパーティライセンス表記の修正
- ポート誤検知の修正: アップグレード時は自サービス停止後に前回ポートを再利用（「全ポート使用中」の誤中断を解消）＋稼働中サービスによる nssm.exe ロック対策
- configure_model.ps1 再実行でナレッジ紐付けが消える不具合を修正（既存の knowledge を保持して上書き）

### v1.0.51 — 幻覚引用の再発防止（履歴汚染対応）
- 会話履歴に文書名が登場しても、参考文書に根拠が無ければ引用禁止（履歴は根拠にならない旨を明記）
- 引用ルールを「根拠がある時のみ末尾に引用欄」に再構成（無条件の引用必須を廃止）
- 実機検証: 汚染された履歴でも架空引用 0 件

### v1.0.50 — 幻覚引用の防止
- ガードレールプロンプトに「参考文書に無い文書名・箇所・数値の創作禁止」を明記
- 実機検証: ナレッジに無い質問（寮）→「該当する記載がありません」と拒否、存在する質問（宿泊費）→15,000円＋出典

### v1.0.49 — 「新機能」ダイアログの非表示
- Open WebUI 0.11 の表示条件（settings.showChangelog 既定 true × バージョン差分）を特定し、ラッパーへのスクリプト注入＋サーバー側設定の両面で無効化
- ja-JP ボタン（「OK、始めましょう！」）対応の自動クローズ

### v1.0.48 — 応答速度の大幅改善
- RAM 連動で LLM＋検索モデルの2モデル同時常駐（質問ごとのモデル切替 +4〜8秒を排除、14GB未満は従来どおり1常駐）
- PC起動時ウォームアップ（タスクスケジューラ ShineosWarmup）で初回質問から再ロード待ちなし
- 背景LLMタスク（タイトル/タグ/追質問候補/入力補完の自動生成）を無効化
- 応答長上限（512トークン）で長文生成による遅延を防止
- Ollama トレイアプリの無効化（見覚えのないアプリが起動するとの誤解防止。常駐は Windowsサービスに一本化）
- インストール時パッチ適用のサイレント失敗を修正（cmd 経由＋install.log 出力方式）

### v1.0.47 — ナレッジRAG自動注入の実装
- Open WebUI 0.11.0 の不具合（モデルの knowledge が RAG 処理へ渡らず社内文書が参照されない）をパッチで修正し、質問に関連する文書を常に自動注入
- ナレッジコレクションをプリセット3モデルへ自動紐付け（未紐付けだと文書を無視して幻覚の原因）
- Ollama を「通常以下」CPU優先度で実行（AI応答中でも他アプリを優先・アイドル時の速度は低下なし）
- NSSM の永続化（一時フォルダ参照で再起動後にサービス起動不能になる問題を修正）
- Ollama 公式トレイアプリの自動起動を無効化しポート競合によるサービス一時停止を防止
- アンインストール時のモデル残留（SYSTEM プロファイル）とファイル生成サーバー残留を修正

## 2026-08-23

### v1.0.46 — モデル一覧の最適化
- チャットのモデル一覧から bge-m3 / qwen2.5:3b を非表示にし、プリセット3つのみ表示

### v1.0.45 — メモリ使用の自動調整
- num_ctx を RAM に連動（8GB未満=2048 / 8〜16GB=4096 / 16GB以上=8192）
- モデル選択画面とREADMEにメモリ案内を追加

### v1.0.44 — ヘッダー名の変更
- Open WebUI のヘッダー名を「Shineos社内知恵袋」に変更（WEBUI_NAME）

### v1.0.43 — モデル常駐時間の延長
- モデル常駐を10分→60分に（再ロード待ちによる応答遅延を防止）

### v1.0.42 — What's New ダイアログ非表示（初回対応）
- WebView2 への JS 注入で「新機能」ダイアログの抑制を試行（→ v1.0.49 で完全対応）

### v1.0.41 — WebView2 データフォルダ修正
- WebView2 のユーザーデータフォルダを %APPDATA% に明示指定（Program Files 配下では E_ACCESSDENIED で起動失敗する問題を修正）

### v1.0.40 — 接続失敗の修正
- localhost（::1 優先解決）で Open WebUI（IPv4のみ）に接続できない問題を修正（127.0.0.1 明示指定）
- ログ・初回起動フラグを %APPDATA% へ移動

### v1.0.39 — インストールの耐障害性
- ナレッジフォルダ指定をラジオボタン方式に変更（「指定しない」を選択可能に）
- ダウンロードにレジューム（-C -）と全エラー自動再試行を追加

### v1.0.38 — スモールモデル最適化
- システムプロンプトに段階的思考（CoT）と回答形式の使い分けを追加・温度0.2・チャンク300で情報密度向上

### v1.0.37 — 平易なエラーメッセージ・Web検索既定OFF
- 技術用語を排除した平易なエラー表示に変更
- Web検索を既定OFFに（外部送信リスクの初回警告をガイドに追加）

### v1.0.36 — 初回起動ガイド改善
- 「次回からこの案内を表示しない」チェックボックスを追加

### v1.0.35 — ナレッジ指定の柔軟化
- インストーラ画面でナレッジフォルダを任意指定可能に（未指定でもインストール可・bat廃止）

### v1.0.34 — コマンド操作の廃止
- 推奨設定をすべてインストーラが自動適用（PowerShell等のコマンド操作を不要に）

### v1.0.33 — RAG安定化
- ENABLE_ASYNC_EMBEDDING=False でナレッジ登録のキュー停滞を防止
- ドキュメント整理（社内Q&A特化の文言に統一）

### v1.0.32 — 社内知恵袋としての本格化
- ナレッジ自動登録（setup_knowledge）・ハイブリッド検索（BM25+ベクトル）・用途別プリセット（経費精算ガイド/ITヘルプデスク）・ガードレールプロンプト（ハルシネーション抑制・CoT）を導入
- リポジトリ名を shineos-qa-assistant に変更

### v1.0.31 — インストーラ堅牢化
- NSSM登録バグ修正（AppParameters をレジストリ直接書込み）・Ollama自動サービス登録・CIでWebView2アプリを事前ビルド

## 2026-08-21

### v1.0.28 — ポートフォールバック
- 8080占有時は8081以降を自動使用＋ラッパーUI改善（→ v1.0.52 でアップグレード時の挙動を改善）

### v1.0.27 — デスクトップアプリ化
- WebView2 ラッパーアプリを追加＋モデル3択化（7b/3b/1.5b）

### v1.0.25 — アンインストーラ強化
- 「Ollama・モデルも全削除（確認ダイアログ付き）」を追加

### v1.0.24 — インストーラ即死の根本修正
- PS 5.1 の配列引数が空白を含むパスを破壊する問題等を修正＋qwen2.5系にモデル切替

### v1.0.16〜v1.0.22 — インストーラ基盤の安定化
- Python をポータブル方式（NuGet zip）に切替（v1.0.16）
- Ollama 起動を3段構えに強化（v1.0.18）
- ライブログ表示・失敗ステップのダイアログ表示（v1.0.10〜v1.0.21）
- PS 5.1 の引数破壊による各種失敗の根本修正（v1.0.22）

### v1.0.8〜v1.0.15 — 導入の信頼性向上
- 古い Ollama の自動更新・サービス重複登録の回避（v1.0.8/9）
- ファイルロック回避のため更新前の旧サービス停止（v1.0.9）
- インストーラのハング対策（タイムアウト＋旧版アンインストール先行）（v1.0.11）
- ollama pull の stderr キャプチャで中断するバグを修正（v1.0.13）

### v1.0.1〜v1.0.7 — 初期安定化
- Pythonインストール失敗時の診断強化（v1.0.1）
- SignPath によるコード署名を CI に導入（v1.0.2、REST API 直接方式）
- 不完全な既存 Python の自己修復（v1.0.5）

### v1.0.0 — 初版
- 無料ローカルAIインストーラ（Ollama + Open WebUI）として公開
### v2.1.5 実機検証の結果（2026-09-20）

- ~~`/VERYSILENT` が機能しない（インストーラ）~~ → **誤検知・取り下げ**: 当初の検証はGit Bashからexeを直接実行したため、MSYSのパス変換が `/VERYSILENT` 等のスイッチを `C:/Program Files/Git/VERYSILENT` というパスに書き換え、スイッチがインストーラに届いていなかった（`/LOG=` のSetupログにコマンドライン実績があり確定）。**cmd.exe経由で再検証した結果、インストール `/VERYSILENT`（exit 0・完了マーカー）・同バージョン再実行のexit 11・アンインストール `/VERYSILENT`（対話なし・データ保持）すべて仕様どおり動作**を確認。無人展開の宣言要件は満たす
- アンインストール時の「ナレッジ・チャット履歴を削除しますか？」確認は対話時のみ表示（サイレント時はデータ保持）——仕様どおり。実機で「いいえ」選択時のデータ保持と再インストールでの引き継ぎを確認
- 教訓: Git BashからWindowsのインストーラを起動する場合は `cmd //c` 経由にする（MSYSのパス変換対策）
